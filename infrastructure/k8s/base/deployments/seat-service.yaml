apiVersion: apps/v1
kind: Deployment
metadata:
  name: seat-service
  namespace: travelplatform
  labels:
    app: seat-service
    app.kubernetes.io/name: seat-service
    app.kubernetes.io/component: subgraph
spec:
  replicas: 2
  selector:
    matchLabels:
      app: seat-service
  template:
    metadata:
      labels:
        app: seat-service
    spec:
      containers:
        - name: seat-service
          image: ghcr.io/travelplatform/seat-service:latest
          ports:
            - containerPort: 4002
          envFrom:
            - configMapRef:
                name: services-config
            - secretRef:
                name: travelplatform-secrets
          env:
            - name: PORT
              value: "4002"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: travelplatform-secrets
                  key: SEAT_DATABASE_URL
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
