apiVersion: apps/v1
kind: Deployment
metadata:
  name: shuttle-service
  namespace: travelplatform
  labels:
    app: shuttle-service
    app.kubernetes.io/name: shuttle-service
    app.kubernetes.io/component: subgraph
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shuttle-service
  template:
    metadata:
      labels:
        app: shuttle-service
    spec:
      containers:
        - name: shuttle-service
          image: ghcr.io/travelplatform/shuttle-service:latest
          ports:
            - containerPort: 4001
          envFrom:
            - configMapRef:
                name: services-config
            - secretRef:
                name: travelplatform-secrets
          env:
            - name: PORT
              value: "4001"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: travelplatform-secrets
                  key: SHUTTLE_DATABASE_URL
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 4001
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 4001
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
