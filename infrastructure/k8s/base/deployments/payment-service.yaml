apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: travelplatform
  labels:
    app: payment-service
    app.kubernetes.io/name: payment-service
    app.kubernetes.io/component: subgraph
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: ghcr.io/travelplatform/payment-service:latest
          ports:
            - containerPort: 4005
          envFrom:
            - configMapRef:
                name: services-config
            - secretRef:
                name: travelplatform-secrets
          env:
            - name: PORT
              value: "4005"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: travelplatform-secrets
                  key: PAYMENT_DATABASE_URL
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 4005
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 4005
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
