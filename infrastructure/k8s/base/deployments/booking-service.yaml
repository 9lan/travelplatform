apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: travelplatform
  labels:
    app: booking-service
    app.kubernetes.io/name: booking-service
    app.kubernetes.io/component: subgraph
spec:
  replicas: 2
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
    spec:
      containers:
        - name: booking-service
          image: ghcr.io/travelplatform/booking-service:latest
          ports:
            - containerPort: 4004
          envFrom:
            - configMapRef:
                name: services-config
            - secretRef:
                name: travelplatform-secrets
          env:
            - name: PORT
              value: "4004"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: travelplatform-secrets
                  key: BOOKING_DATABASE_URL
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 4004
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 4004
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
