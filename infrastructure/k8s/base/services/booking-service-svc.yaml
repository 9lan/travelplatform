apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: travelplatform
  labels:
    app: booking-service
spec:
  type: ClusterIP
  ports:
    - port: 4004
      targetPort: 4004
      protocol: TCP
      name: http
  selector:
    app: booking-service
