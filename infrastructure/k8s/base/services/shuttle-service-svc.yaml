apiVersion: v1
kind: Service
metadata:
  name: shuttle-service
  namespace: travelplatform
  labels:
    app: shuttle-service
spec:
  type: ClusterIP
  ports:
    - port: 4001
      targetPort: 4001
      protocol: TCP
      name: http
  selector:
    app: shuttle-service
