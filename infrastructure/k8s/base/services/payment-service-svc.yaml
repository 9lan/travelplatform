apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: travelplatform
  labels:
    app: payment-service
spec:
  type: ClusterIP
  ports:
    - port: 4005
      targetPort: 4005
      protocol: TCP
      name: http
  selector:
    app: payment-service
