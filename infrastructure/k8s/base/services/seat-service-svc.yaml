apiVersion: v1
kind: Service
metadata:
  name: seat-service
  namespace: travelplatform
  labels:
    app: seat-service
spec:
  type: ClusterIP
  ports:
    - port: 4002
      targetPort: 4002
      protocol: TCP
      name: http
  selector:
    app: seat-service
