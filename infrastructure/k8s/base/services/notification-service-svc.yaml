apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: travelplatform
  labels:
    app: notification-service
spec:
  type: ClusterIP
  ports:
    - port: 4007
      targetPort: 4007
      protocol: TCP
      name: http
  selector:
    app: notification-service
