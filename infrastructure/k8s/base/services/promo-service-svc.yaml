apiVersion: v1
kind: Service
metadata:
  name: promo-service
  namespace: travelplatform
  labels:
    app: promo-service
spec:
  type: ClusterIP
  ports:
    - port: 4006
      targetPort: 4006
      protocol: TCP
      name: http
  selector:
    app: promo-service
